<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='/stylesheets/userView.css' />
    <link rel='stylesheet' href='/stylesheets/backgroundImage.css' />
  </head>
  <body>
    <div class="content">
        <h1><%= title %></h1>
        <%- include('header') %>
        <h2>調整百分比</h2>
        <hr>
        老師可以獲得的百分比（％）<br>
        <form action="/moneyPercentage/" method="post">
            <input type="number" name="percentage" min="0" max="100" id="moneyPercentage" value="<%= moneyPercentage.percentage %>" onchange="rangeChecks(this.value,this.id);" required>
            <input type="submit" class="registerbtn" value="提交">
        </form>


        <% if (messages) { %>
            <section class="messages">
            <% messages.forEach(function(message) { %>
                <p><b><%= message %></b></p>
            <% }); %>
            </section>
        <% } %>
    </div>
  </body>

  <script>
    function rangeChecks(val,id){
        if(val<0){
            alert("百分比不能為負數，請重新輸入");
            document.getElementById(id).value=50; 
        }
    }

  </script>
   
</html>
